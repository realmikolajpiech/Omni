I will implement the memory optimizations, focusing on the main 6GB "brain" while keeping the fast model instant and separate.

### **Answer: Should the fast model be in the server?**
**No, leave it as it is.**
Moving the fast model to the server would introduce a small delay (network latency) for every action. To keep your computer control "snappy" and instant, it's best to keep the fast model running directly inside the main application. The memory optimizations below will handle the heavy lifting (the 6GB part) without slowing down the fast model.

### **Implementation Plan**

1.  **Implement Idle Shutdown (The 6GB Fix)**
    *   Modify `src/services/llm/model_manager.py` to track when the main model is used.
    *   Create a background monitor that **unloads the main model** (`llama-server`) after **10 minutes** of inactivity.
    *   This will reclaim the full ~6GB of RAM when you aren't actively chatting.

2.  **Optimize Server Configuration**
    *   Modify `start_model_server.sh` to:
        *   Reduce "short-term memory" (Context Window) from **8,192** to **4,096** tokens (saves ~1GB).
        *   Enable **KV Cache Quantization** (Q8_0) to compress the remaining memory usage.
    *   This ensures that even when the model *is* running, it uses significantly less RAM (~4GB instead of 6GB).

3.  **Verify & Safety**
    *   Ensure the fast model remains untouched and responsive.
    *   Verify that the main model automatically restarts when you ask a new question after the shutdown.
