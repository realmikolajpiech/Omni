I will modify `src/app/main.py` to implement a custom keyboard listener using `pynput`.

The current implementation uses `GlobalHotKeys` which expects standard hotkey combinations (like `Option+Command+Space`), but does not support triggering on modifier keys alone (like just `Option+Command`).

I will replace the `GlobalHotKeys` implementation with a `pynput.keyboard.Listener` that:
1.  Tracks the state of pressed keys.
2.  Detects when **only** `Option` and `Command` are held down together.
3.  Triggers `toggle_omni()` when one of them is released (ensuring no other keys were pressed in between, to avoid conflicting with other shortcuts like `Cmd+Opt+S`).
4.  Retains support for the `Option + Space` shortcut as a fallback/alternative.

**Changes:**
-   **File:** `src/app/main.py`
-   **Action:** Replace the macOS hotkey registration block (lines 245-271) with the new `Listener` logic.
