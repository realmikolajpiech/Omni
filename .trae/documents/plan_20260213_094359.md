I will use the provided password to clean up the duplicate servers and ensure they don't respawn.

### **Plan: Cleanup & Enforce Single Instance**

1.  **Kill Zombie Processes (Sudo)**
    *   Use `echo "miki" | sudo -S pkill -f llama-server` to forcefully terminate all running `llama-server` instances.
    *   This immediately reclaims the ~1.2 GB of wasted RAM.

2.  **Harden `start_model_server.sh`**
    *   Add a check inside the script: If port 8081 is already in use (checked via `lsof -i :8081`), the script will exit immediately instead of starting a second server.
    *   This acts as a "Fail-Safe" in case the Python lock fails.

3.  **Outcome**
    *   **Immediate Memory Drop**: ~1.2 GB saved.
    *   **Stability**: Guaranteed single instance of the AI brain.
