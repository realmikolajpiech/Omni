# Core
flask
requests
httpx[http2]
simpleeval
embed-anything
lancedb
memvid_sdk
PyQt6
PyQt6-WebEngine
Pillow
watchdog
packaging
pypdf
python-docx
openpyxl
python-pptx

# Keyboard handling
keyboard

# AI / LLM (Groq + xAI via OpenAI-compatible API)
openai
groq
huggingface_hub[hf_xet]

# Voice (ASR & TTS)
numpy
scipy
soundfile
sounddevice
openwakeword
onnxruntime
edge-tts
langdetect

# Utils
pytz

# Windows specific
pywin32; sys_platform == 'win32'
BlurWindow; sys_platform == 'win32'

# macOS specific
pyobjc; sys_platform == 'darwin'

qrcode
